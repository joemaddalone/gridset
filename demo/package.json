{
  "scripts": {
    "start": "snowpack dev --reload --clean --no-bundle",
    "build": "snowpack build",
    "format": "prettier --write \"src/**/*.js\"",
    "lint": "prettier --check \"src/**/*.js\""
  },
  "devDependencies": {
    "prettier": "^2.0.5",
    "snowpack": "^2.11.0"
  },
  "dependencies": {
    "gridset": "file:.."
  }
}
